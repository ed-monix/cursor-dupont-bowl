.cache/
__pycache__/
.pytest_cache/
*.pyc

# generated viewer output
web/viewer.html

# stray virtualenvs from tooling
.venv/

# secrets — never commit keys (GROK_API_KEY lives in the environment)
.env

# derived GM packs (rebuild: python scripts/gm_pack.py --week N)
state/weeks/*/packs/

# local Grok Bot paste exports (never copy onto the shared Bot disk)
bot-prompts/
