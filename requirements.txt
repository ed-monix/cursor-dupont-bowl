# DuPont Bowl engine — runtime + test dependencies.
# Install into a single virtualenv:
#   python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# The scoreboard uses only the Python standard library (http.server), so Flask
# is intentionally NOT a dependency.
requests>=2.31
pytest>=8.0
