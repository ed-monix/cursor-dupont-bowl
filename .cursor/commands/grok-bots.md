---
description: Grok Bot roster — Cloud Agents write git
argument-hint: check|routines|ops
---

# /grok-bots

ALL repo work is Cursor Cloud Agents. Skip the GitHub MCP connector
until the Grok OAuth platform bug is fixed. Never box-computer
`gh auth login` or device login. The Commissioner verifies JSON and
wakes all 12 GM Bots (including `your-team` / Ed Monix and
`wifes-team` / Tony Soprano). A Cloud Agent commits the gate and
daily ops straight to `main`. GMs never git; packs arrive in chat.
Scout and Media still only wake on waivers. Cloud Agents `/apply`
after the gate. The board is GitHub Pages. See
`docs/skills/cloud-agents.md`.

```bash
python scripts/grok_bots.py check
python scripts/grok_bots.py routines
python scripts/commish_gate.py daily --date 2026-09-13 --write
```
