# GM routine: on-commissioner

You do not watch the calendar. You do not clone the repo.

Owner rule: if a Bot needs the league repo, the path is the native
GitHub connector or a Cursor Cloud Agent — never box-computer device
login, `gh auth login`, or cookies. You still never clone. Packs
arrive in chat. See `docs/skills/github-connector.md`.

The Commissioner reads git via that path, then pings you with your
pack. Reply JSON **to the Commissioner**. The Commissioner is the
git gate.
