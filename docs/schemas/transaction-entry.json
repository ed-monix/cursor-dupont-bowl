{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "transaction-entry.json",
  "title": "Transaction Log Entry",
  "description": "One line of state/transactions.jsonl (PLAN.md §4 step 6 / §8): append-only, one JSON object per applied action (add/drop/trade/waiver claim).",
  "type": "object",
  "required": ["timestamp", "team", "action", "players", "bid", "reasoning", "status"],
  "properties": {
    "timestamp": {
      "type": "string",
      "description": "ISO 8601 timestamp of when the transaction was applied."
    },
    "team": {
      "type": "string",
      "description": "Team slug the transaction was applied to (matches teams/<slug>)."
    },
    "action": {
      "type": "string",
      "description": "Kind of transaction, e.g. 'add', 'drop', 'waiver_claim', 'trade'.",
      "enum": ["add", "drop", "waiver_claim", "trade"]
    },
    "players": {
      "type": "array",
      "description": "Player ids involved (adds and drops together for a claim; both sides for a trade).",
      "items": {"type": "string"}
    },
    "bid": {
      "type": ["integer", "null"],
      "description": "FAAB dollars spent, or null when the action isn't a waiver claim (e.g. a free drop or trade).",
      "minimum": 0
    },
    "reasoning": {
      "type": "string",
      "description": "In-character reasoning logged with the transaction (from the agent's decision JSON, or the commissioner's ruling)."
    },
    "status": {
      "type": "string",
      "description": "Outcome of the transaction as applied.",
      "enum": ["applied", "rejected", "flagged"]
    }
  },
  "additionalProperties": false
}
